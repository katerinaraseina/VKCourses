Домашнее задание
ДЗ до вторника (01.06.2021)
Linux terminal
Вот сценарий который надо уметь делать

1) Посмотреть где я
 pwd

2) Создать папку
mkdir имя_папки

3) Зайти в папку
cd имя_папки 

4) Создать 3 папки
mkdir имя_папки имя_папки имя_папки

5) Зайти в любую папку
cd имя_папки 
cd путь_к_папке

6) Создать 5 файлов (3 txt, 2 json)
touch File1.txt  File2.txt File3.txt File4.json File5.json

7) Создать 3 папки
mkdir имя_папки имя_папки имя_папки

8. Вывести список содержимого папки
ls (если находимся в данной папке)
ls наименование_папки

9) + Открыть любой txt файл
nano наименование_файла

10) + написать туда что-нибудь, любой текст.
написать текст 

11) + сохранить и выйти.
Ctrl+Y  -> Enter

12) Выйти из папки на уровень выше
cd ..
—
13) переместить любые 2 файла, которые вы создали, в любую другую папку.
mv наименование _файла наименование _папки
mv наименование _файла ../наименование _папки

14) скопировать любые 2 файла, которые вы создали, в любую другую папку.
cp наименовнаие_файла наименование_файла “путь_к_папке”

15) Найти файл по имени
find . -name Наименовнаие _файла
find . -iname Наименование _файла

16) просмотреть содержимое в реальном времени (команда grep) изучите как она работает.
просмотреть содержимое в реальном времени:   tail -f наименование_файла
осуществить поиск по шаблону:  
grep шаблон  наименование_файла
grep -i  шаблон   наименование_файла (исключает формат букв)
grep -R  шаблон наименование_папки/ 
grep -R  шаблон . (поиск в текущей директории)

17) вывести несколько первых строк из текстового файла
head наименование_файла  - выводит 10 первых строк 
head -3 наименовнаие_файла -выводит три первые строки файла

18) вывести несколько последних строк из текстового файла
tail  Наименование_файла - выводить последних 10 строк
tail -3 Наименование_файла  - выводит  три последние  строки (кол-во указанных)

19) просмотреть содержимое длинного файла (команда less) изучите как она работает.
less наименование_файл 

20) вывести дату и время
date
=========

Задание *
1) Отправить http запрос на сервер.
https://api.quarantine.country/api/v1/regions

curl -i https://api.quarantine.country/api/v1/regions

2) Написать скрипт который выполнит автоматически пункты 3, 4, 5, 6, 7, 8, 13

#!/bin/bash
# Зайти в  папку
cd part1
# Создать три папки
mkdir script1 script1.1 script1.2
# Зайти в любую папку
cd script1
# Создать 5 файлов (3txt 2json)
touch fscript1.txt fscript2.txt fscript3.txt fscript4.json fscript5.json
# Создать 3 папки
mkdir script2 script3 script4
# Вывести список содержимомго папки
ls
# Переместить два файла в любую другую папку
mv fscript1.txt fscript2.txt script2


=========
bash наименование _файла (скрипта)
